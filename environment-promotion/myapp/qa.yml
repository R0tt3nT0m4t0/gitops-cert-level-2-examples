apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: qa
  namespace: qa
spec:
  template:      
    metadata:
      name: qa
    spec:
      project: default
      source:
        repoURL: https://github.com/R0tt3nT0m4t0/gitops-cert-level-2-examples.git
        targetRevision: HEAD
        path: ./environment-promotion/myapp/qa.yml
      destination:
        server: https://kubernetes.default.svc
        namespace: qa
      syncPolicy:
        syncOptions:
          - CreateNamespace=true  
        automated:
          prune: true 
          selfHeal: true 
