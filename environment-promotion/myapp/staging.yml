apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: staging
  namespace: staging
spec:
  template:      
    metadata:
      name: staging
    spec:
      project: default
      source:
        repoURL: https://github.com/R0tt3nT0m4t0/gitops-cert-level-2-examples.git
        targetRevision: HEAD
        path: ./environment-promotion/myapp/staging.yml
      destination:
        server: https://kubernetes.default.svc
        namespace: staging
      syncPolicy:
        syncOptions:
          - CreateNamespace=true  
        automated:
          prune: true 
          selfHeal: true 
